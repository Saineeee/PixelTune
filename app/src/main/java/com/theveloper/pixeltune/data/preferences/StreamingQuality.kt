package com.theveloper.pixeltune.data.preferences

enum class StreamingQuality(val title: String) {
    DATA_SAVER("Data Saver (Lowest)"),
    NORMAL("Normal (Standard)"),
    HIGH_RES("High-Res (Best)")
}
